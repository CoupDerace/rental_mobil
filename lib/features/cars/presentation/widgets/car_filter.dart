import 'package:flutter/material.dart';

class CarFilter extends StatelessWidget {
  const CarFilter({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox.shrink();
  }
}
